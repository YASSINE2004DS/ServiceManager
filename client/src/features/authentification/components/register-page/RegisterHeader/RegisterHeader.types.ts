/**
 * @interface RegisterHeaderProps
 * @description Props interface for the RegisterHeader component
 */
export interface RegisterHeaderProps {
    // No props needed - component manages its own data
}

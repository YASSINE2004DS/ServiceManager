/**
 * @interface CompanyValue
 * @description Interface for company core values
 */
export interface CompanyValue {
    title: string;
    description: string;
    icon: string;
}

/**
 * @interface AboutValuesProps
 * @description Props interface for the AboutValues component
 */
export interface AboutValuesProps {
    // No props needed - component manages its own data
}

/**
 * @interface AboutMissionVisionProps
 * @description Props interface for the AboutMissionVision component
 */
export interface AboutMissionVisionProps {
    // No props needed - component manages its own data
}

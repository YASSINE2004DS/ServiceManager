/**
 * @interface AboutOverviewProps
 * @description Props interface for the AboutOverview component
 */
export interface AboutOverviewProps {
    // No props needed - component manages its own data
}

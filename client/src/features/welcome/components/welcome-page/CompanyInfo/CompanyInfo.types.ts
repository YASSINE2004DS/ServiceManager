import type { CompanyInfo } from '../../../pages/Welcome/Welcome.types';

/**
 * @interface CompanyInfoProps
 * @description Props interface for the CompanyInfo component
 */
export interface CompanyInfoProps {
    // No props needed - component manages its own data
}
